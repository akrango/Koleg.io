@{
    ViewBag.Title = "Home Page";
}
<head>
    <script>
        window.onload = function () {
            let slideIndex = 1;
            showSlides(slideIndex);

            function plusSlides(n) {
                showSlides(slideIndex += n);
            }

            function currentSlide(n) {
                showSlides(slideIndex = n);
            }

            function showSlides(n) {
                let i;
                let slides = document.getElementsByClassName("mySlides");
                let dots = document.getElementsByClassName("dot");

                if (n > slides.length) { slideIndex = 1 }
                if (n < 1) { slideIndex = slides.length }

                for (i = 0; i < slides.length; i++) {
                    slides[i].style.display = "none";
                }

                for (i = 0; i < dots.length; i++) {
                    dots[i].className = dots[i].className.replace(" active", "");
                }

                slides[slideIndex - 1].style.display = "block";
                dots[slideIndex - 1].className += " active";

                // Increase the slideIndex for the next iteration
                slideIndex++;

                // Call the function recursively after a delay
                setTimeout(function () {
                    showSlides(slideIndex);
                }, 1500); // Adjust the delay as needed
            }
        }
    </script>
    <style>
        main{
            margin-top:30px;
        }
        * {
            box-sizing: border-box
        }

        /* Slideshow container */
        .slideshow-container {
            max-width: 700px;
            position: relative;
            margin: auto;
            text-align: center; /* Center the child elements */
        }

        /* Hide the images by default */
        .mySlides {
            display: none;
        }

            /* Full-width images with number and caption text */
            .mySlides img {
                width: 50%;
                margin: auto; /* Center the image */
                display: block;
            }


        /* Next & previous buttons */
        .prev, .next {
            cursor: pointer;
            position: absolute;
            top: 50%;
            width: auto;
            margin-top: -22px;
            padding: 16px;
            color: white;
            font-weight: bold;
            font-size: 18px;
            transition: 0.6s ease;
            border-radius: 0 3px 3px 0;
            user-select: none;
        }

        /* Position the "next button" to the right */
        .next {
            right: 0;
            border-radius: 3px 0 0 3px;
        }

            /* On hover, add a black background color with a little bit see-through */
            .prev:hover, .next:hover {
                background-color: rgba(0,0,0,0.8);
            }

        /* Caption text */
        .text {
            color: #f2f2f2;
            font-size: 15px;
            padding: 8px 12px;
            position: absolute;
            bottom: 8px;
            width: 100%;
            text-align: center;
        }

        /* Number text (1/3 etc) */
        .numbertext {
            color: #f2f2f2;
            font-size: 12px;
            padding: 8px 12px;
            position: absolute;
            top: 0;
        }

        /* The dots/bullets/indicators */
        .dot {
            cursor: pointer;
            height: 15px;
            width: 15px;
            margin: 0 2px;
            background-color: #bbb;
            border-radius: 50%;
            display: inline-block;
            transition: background-color 0.6s ease;
        }

            .active, .dot:hover {
                background-color: #717171;
            }

        /* Fading animation */
        .fade {
            animation-name: fade;
            animation-duration: 1.5s;
        }

        @@-webkit-keyframes fade {
            from {
                opacity: .4
            }

            to {
                opacity: 1
            }
        }

        #title {
            animation: bounce 2s infinite;
        }

        @@keyframes bounce {
            0%, 20%, 50%, 80%, 100% {
                transform: translateY(0);
            }

            40% {
                transform: translateY(-20px);
            }

            60% {
                transform: translateY(-10px);
            }
        }

        /* Add emojis to the quote */
        #fun-quote {
            font-size: 18px;
            margin-top: 20px;
        }
    </style>
</head>
<main >
    <section class="row text-center" aria-labelledby="aspnetTitle">
        <h1 id="title">Welcome to Koleg.io</h1>
@*        <p class="lead">We only want you here if you can help a Kolega out</p>
*@        @if (!User.Identity.IsAuthenticated)
        {
            <span class="lead">Become a registered FINKASH</span>

            <p>@Html.ActionLink("HERE", "Register", "Account", new { }, new { @class = "btn btn-info" })</p>
        }

    </section>

    <div class="container mt-4">
        <div class="row">

            <!-- Slideshow container (on the left) -->
            <div class="col-md-6 mb-4">
                <div class="slideshow-container" style="max-width: 400px;">
                    <!-- Full-width images with number and caption text -->
                    <div class="mySlides fade">
                        <div class="numbertext">1 / 3</div>
                        <img src="~/Content/Images/slideshow1.1.png" style="width: 400px; height: 350px">
                   </div>

                    <div class="mySlides fade">
                        <div class="numbertext">2 / 3</div>
                        <img src="~/Content/Images/slideshow4.png" style="width: 400px; height: 350px">
                    </div>

                    <div class="mySlides fade">
                        <div class="numbertext">3 / 3</div>
                        <img src="~/Content/Images/slideshow3.png" style="width:400px;height:350px">
                    </div>

                   
                </div>
                <br>
                <!-- The dots/circles -->
                <div style="text-align:center">
                    <span class="dot" onclick="currentSlide(1)"></span>
                    <span class="dot" onclick="currentSlide(2)"></span>
                    <span class="dot" onclick="currentSlide(3)"></span>
                </div>
            </div>

            <!-- Cards container (on the right) -->
            <div class="col-md-6 mb-4">
                <!-- Card 1 -->
                <div class="card" style="background-color: #aec6cf; color: #000; ">
                    <div class="card-body">
                        <h5 class="card-title">Academic Support</h5>
                        <p class="card-text">Explore the subjects your Kolegi can assist you with during your college journey. Receive personalized guidance and support to excel in your studies.</p>
                    </div>
                </div>

                <!-- Card 2 -->
                <div class="card mt-4" style="background-color: #d8bfd8; color: #000;">
                    <div class="card-body">
                        <h5 class="card-title">Collaborative File Sharing</h5>
                        <p class="card-text">Empower your Kolegi community by contributing to the knowledge pool. Easily upload and share valuable files to aid your peers in their academic endeavors.</p>
                    </div>
                    @Html.ActionLink("Discover More", "Index", "Subjects", new { id = 0 }, new { @class = "btn btn-info" })
                </div>
            </div>
        </div>
    </div>
</main>

@section Scripts{
    <script>
         $(".js-login").on("click", function () {
                var isAuthenticated = @User.Identity.IsAuthenticated.ToString().ToLower();
                console.log(isAuthenticated)

                if (isAuthenticated !== true) {
                    bootbox.confirm({
                        message: "You need to be logged in to rate this file!",
                        closeButton: false,
                        buttons: {
                            confirm: {
                                label: 'Log in',
                                className: 'btn-success',
                            },
                            cancel: {
                                label: 'Cancel',
                                className: 'btn-secondary'
                            }
                        },
                        callback: function (result) {
                            if (result) {
                                var currentUrl = window.location.href;
                                var das = currentUrl.split("/Subjects")
                                var returnUrl = "/Subjects/Index/0"
                                window.location.href = '/Account/Login?returnUrl=' + returnUrl;
                            }
                        }
                    })
                }
            })
    </script>    
}
